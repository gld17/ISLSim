# Plan Quiz

请回答以下两个问题：

## Q1

`point_test.py` 的标准输出应该包含什么？

A. 完整的链路预算表格（距离、SNR、调制格式、带宽等）
B. 仅包含一个带宽数值（`total_bw_gbps`）
C. JSON 格式的链路结果对象
D. 带宽数值以及详细的配置信息说明

## Q2

本次任务的边界条件中，以下哪个是**不允许**的？

A. 新增 `point_test.py` 脚本
B. 使用 `argparse` 处理命令行参数
C. 修改 `sweep.py` 的内容
D. 使用 `config.py` 的 `load_config_from_json()` 加载配置
